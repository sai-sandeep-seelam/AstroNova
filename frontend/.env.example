# Frontend Environment Variables
VITE_API_URL=http://localhost:5000/api

# Optional: Cesium Ion Token for better map features
# Get it from: https://ion.cesium.com/tokens

# Note: API keys in frontend are kept minimal.
# Most API calls go through the backend for security.
